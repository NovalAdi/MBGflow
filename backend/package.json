{
  "name": "mbgflow-backend",
  "version": "1.0.0",
  "description": "Backend API server for MBGflow using Node.js, Express and MySQL",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/update-db-version.js && wrangler deploy"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "hono": "^4.0.0",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
